# ============================================================================
# FIREBASE CONFIGURATION
# ============================================================================
# Get these values from Firebase Console → Project Settings → General tab
# Reference: https://firebase.google.com/docs/projects/learn-more#project-settings

# Firebase Web API Key
VITE_FIREBASE_API_KEY=your_firebase_api_key_here

# Firebase Auth Domain (format: projectid.firebaseapp.com)
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com

# Firebase Project ID
VITE_FIREBASE_PROJECT_ID=your_project_id

# Firebase Storage Bucket (format: projectid.appspot.com)
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com

# Firebase Messaging Sender ID
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id

# Firebase App ID
VITE_FIREBASE_APP_ID=your_app_id

# ============================================================================
# FIREBASE ADMIN SDK (Server-side)
# ============================================================================
# Get these values from Firebase Console → Project Settings → Service Accounts
# WARNING: Keep the private key SECRET. Never commit it to git!
# Reference: https://firebase.google.com/docs/admin/setup

# Firebase Admin Project ID
FIREBASE_PROJECT_ID=your_project_id

# Firebase Admin Private Key (entire key including BEGIN/END markers)
# Format: "-----BEGIN PRIVATE KEY-----\n...content...\n-----END PRIVATE KEY-----\n"
FIREBASE_PRIVATE_KEY=your_private_key_here

# Firebase Admin Client Email (service account email)
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your_project_id.iam.gserviceaccount.com

# ============================================================================
# AUTHORIZED EMAIL ACCOUNTS
# ============================================================================
# Comma-separated list of Gmail accounts allowed to access admin panel
# Supports specific emails: user@gmail.com,admin@example.com
# Or domain wildcards: @company.com (all users from this domain)
# Examples:
#   VITE_AUTHORIZED_EMAILS=admin@gmail.com,moderator@company.com
#   VITE_AUTHORIZED_EMAILS=admin@gmail.com,@company.com

VITE_AUTHORIZED_EMAILS=your-email@gmail.com,another-admin@gmail.com

# ============================================================================
# CLOUDFLARE R2 STORAGE CONFIGURATION
# ============================================================================
# Get these values from Cloudflare dashboard → R2 → API tokens
# Reference: https://developers.cloudflare.com/r2/api/s3/tokens/

# R2 Access Key ID
R2_ACCESS_KEY_ID=your_r2_access_key_here

# R2 Secret Access Key
R2_SECRET_ACCESS_KEY=your_r2_secret_key_here

# Cloudflare Account ID (found in Cloudflare dashboard)
R2_ACCOUNT_ID=your_account_id_here

# R2 Bucket Name (the name you created for your bucket)
R2_BUCKET_NAME=your_bucket_name_here

# R2 Public URL (optional but recommended)
# Format: https://your-custom-domain.com or https://bucket-name.your-account-id.r2.cloudflarestorage.com
# If not set, will use: https://R2_BUCKET_NAME.R2_ACCOUNT_ID.r2.cloudflarestorage.com
R2_PUBLIC_URL=https://your-custom-domain-or-default.com

# ============================================================================
# APPLICATION ENVIRONMENT
# ============================================================================

# Node.js environment (development, production, staging)
NODE_ENV=production

# Server port (optional, defaults to 3000)
PORT=3000

# ============================================================================
# PING MESSAGE (Optional)
# ============================================================================
# Custom message for /api/ping endpoint (for testing)
PING_MESSAGE=ping

# ============================================================================
# DEPLOYMENT NOTES
# ============================================================================
# For Netlify:
#   1. Go to Site settings → Build & deploy → Environment
#   2. Add each variable above
#   3. For multi-line values like FIREBASE_PRIVATE_KEY, include \n characters
#
# For VPS (Docker/Linux):
#   1. Create .env file with all values from this template
#   2. Make sure .env is NOT committed to git
#   3. Load environment before running: source .env && npm start
#   4. Or use: docker run --env-file .env <image>
#
# For Local Development:
#   1. Copy this file to .env.local
#   2. Fill in your actual values
#   3. Never commit .env.local to git
#
# Security Best Practices:
#   - Never commit .env files to git
#   - Use strong, unique credentials
#   - Rotate R2 API tokens periodically
#   - Use Firebase service account key rotation
#   - Keep the FIREBASE_PRIVATE_KEY secret and secure
#   - Use environment-specific values (separate test/prod keys)
